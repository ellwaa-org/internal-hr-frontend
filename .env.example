# Copy to .env for local development. Do not commit .env.

# API base path (must include /api — matches OpenAPI servers.url)
VITE_API_URL=/api

# Dev-server proxy target (Vite rewrites /api → this host)
VITE_API_PROXY_TARGET=https://hr-api-staging.ellwaa.com
